// This file is intentionally left blank to disable middleware,
// as it's not compatible with `output: 'export'` for Capacitor builds.
// The presence of an exported `middleware` function causes build errors.
